name: Deploy to DigitalOcean Droplet

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      - name: Configurar SSH
        run: |
          mkdir -p ~/.ssh
          printf "%s" "${{ secrets.DROPLET_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p "${{ secrets.DROPLET_PORT }}" -H "${{ secrets.DROPLET_HOST }}" > ~/.ssh/known_hosts
          chmod 644 ~/.ssh/known_hosts

      - name: Diagnóstico SSH
        run: |
          ssh -o PreferredAuthentications=publickey -o PasswordAuthentication=no \
            -i ~/.ssh/deploy_key -p "${{ secrets.DROPLET_PORT }}" \
            "${{ secrets.DROPLET_USER }}@${{ secrets.DROPLET_HOST }}" \
            "echo Conexion SSH exitosa"

      - name: Crear carpeta destino en el Droplet
        run: |
          ssh -i ~/.ssh/deploy_key -p "${{ secrets.DROPLET_PORT }}" \
            "${{ secrets.DROPLET_USER }}@${{ secrets.DROPLET_HOST }}" \
            "mkdir -p /var/www/html/ghholamundo"

      - name: Deploy con rsync
        run: |
          rsync -avz --delete \
            --exclude ".git" \
            --exclude ".github" \
            -e "ssh -i ~/.ssh/deploy_key -p ${{ secrets.DROPLET_PORT }}" \
            ./ \
            "${{ secrets.DROPLET_USER }}@${{ secrets.DROPLET_HOST }}:/var/www/html/ghholamundo"

      - name: Post-deploy opcional
        run: |
          ssh -i ~/.ssh/deploy_key -p "${{ secrets.DROPLET_PORT }}" \
            "${{ secrets.DROPLET_USER }}@${{ secrets.DROPLET_HOST }}" \
            "cd /var/www/html/ghholamundo && echo 'Deploy completado'"
